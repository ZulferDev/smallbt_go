--- internal/strategy/evaluator/evaluator.go
+++ internal/strategy/evaluator/evaluator.go
@@ -256,9 +257,10 @@
 			e.values[name] = 0
 		}
 		
-		if e.context.BarIndex == 0 {
-			fmt.Printf("[DEBUG] Bar 0: About to calculate composite indicator '%s'\n", name)
+		fmt.Printf("[DEBUG] Stored composite indicator '%s' value: %.4f (bar %d)\n", name, e.values[name], e.context.BarIndex)
+		if e.context.BarIndex == 0 {
 			fmt.Printf("[DEBUG] Stored composite indicator '%s' value: %.4f\n", name, e.values[name])
+		}
 	}
 
 	return nil
